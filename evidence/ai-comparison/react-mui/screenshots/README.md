# SLOT — AWAITING HUMAN EXECUTION

> This directory is intentionally empty. Drop screenshots only from the controlled run described in `docs/ai-comparison-protocol.md`.

Before/after screenshots at 1440, 768, and 375 (PNG). Filenames: `<state>-<viewport>.png`.
